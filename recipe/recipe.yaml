schema_version: 1

context:
  name: vit-pytorch
  version: "1.12.5"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 30bc16af21adfb794e28e1b32d7d10693bd4751c0a8c9371d8df5ab3583e4fcf

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools >=61
  run:
    - einops >=0.7.0
    - python >=${{ python_min }}
    - pytorch >=1.10
    - torchvision

tests:
  - python:
      imports:
        - vit_pytorch
      pip_check: true
      python_version: ${{ python_min }}.*
  - files:
      source:
        - tests/test.py
    requirements:
      run:
        - pytest
        - python ${{ python_min }}.*
    script:
      - pytest tests/*.py

about:
  homepage: https://github.com/lucidrains/vit-pytorch
  license: MIT
  license_file: LICENSE
  summary: Vision Transformer (ViT) - Pytorch

extra:
  recipe-maintainers:
    - weiji14
